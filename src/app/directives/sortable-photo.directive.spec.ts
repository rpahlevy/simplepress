import { SortablePhotoDirective } from './sortable-photo.directive';

describe('SortablePhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new SortablePhotoDirective();
    expect(directive).toBeTruthy();
  });
});
