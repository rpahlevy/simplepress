import { SortableAlbumDirective } from './sortable-album.directive';

describe('SortableAlbumDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableAlbumDirective();
    expect(directive).toBeTruthy();
  });
});
